% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_testConsensus.R
\name{heatmap.plot}
\alias{heatmap.plot}
\title{Plot Hierarchical Clustering Heatmap for Consensus Genes}
\usage{
heatmap.plot(
  heatmap_data,
  colors = (grDevices::colorRampPalette(c("#2E4057", "#66A182", "#EDAE49")))(64),
  dendrogram = "both",
  show_dendrogram = c(FALSE, TRUE),
  scale = "row",
  custom_colors = c(`1` = "#9E363A", `0` = "#467599"),
  margins = c(60, 100, 40, 20),
  grid_color = "white",
  grid_width = 1e-05,
  branches_lwd = 0.4,
  fontsize_row = 12,
  fontsize_col = 5,
  scale_fill_gradient_fun = NULL,
  limits = NULL,
  na.value = "grey50",
  cellnote = NULL,
  cellnote_size = NULL,
  cellnote_textposition = "middle center",
  key.title = "",
  key.xlab = "Value",
  key.ylab = "Frequency"
)
}
\arguments{
\item{heatmap_data}{A list with:
- `matrix`: Numeric matrix of expression values (genes x samples or vice-versa).
- `hc_rows`: Hierarchical clustering object for rows (e.g., output of `hclust`).
- `hc_cols`: Hierarchical clustering object for columns (e.g., output of `hclust`).
- `side_colors` (optional): Vector or matrix for coloring sample columns.}

\item{colors}{Color palette (vector of colors or function) for the heatmap.}

\item{dendrogram}{Which dendrograms to display. One of `"both"`, `"row"`, `"column"`, `"none"`. Default: `"both"`.}

\item{show_dendrogram}{Logical vector of length 2; whether to show row and column dendrograms (e.g., `c(FALSE, TRUE)`).}

\item{scale}{How to scale the data: `"none"`, `"row"`, or `"column"`. Default: `"row"`.}

\item{custom_colors}{Named vector for coloring groups in side bar (e.g., `c("1"="#2e1457", "0"="#66a182")`).}

\item{margins}{Numeric vector of margins (top, right, bottom, left).}

\item{grid_color}{Color of the heatmap grid lines.}

\item{grid_width}{Width of the grid lines.}

\item{branches_lwd}{Line width for dendrogram branches.}

\item{fontsize_row}{Font size for row labels.}

\item{fontsize_col}{Font size for column labels.}

\item{scale_fill_gradient_fun}{Custom function for color scaling (advanced, optional).}

\item{limits}{Numeric vector of length 2, setting min and max of color scale (optional).}

\item{na.value}{Color for NA values.}

\item{cellnote}{Optional matrix of text to display in each cell.}

\item{cellnote_size}{Font size for cell notes (if provided).}

\item{cellnote_textposition}{Position for cell note text (if provided).}

\item{key.title}{Title for the heatmap color key.}

\item{key.xlab}{X-axis label for the heatmap color key.}

\item{key.ylab}{Y-axis label for the heatmap color key.}
}
\value{
An interactive heatmaply htmlwidget visualizing hierarchical clustering and expression values.
}
\description{
Generates an interactive or static heatmap with hierarchical clustering for a matrix of gene expression values,
typically representing consensus genes across samples. This function wraps `heatmaply::heatmaply` with customization options.
}
\details{
This function returns an interactive heatmap, allowing zoom, tooltip and dynamic exploration of gene/sample clustering.
}
\examples{
\dontrun{
p <- heatmap.plot(heatmap_data)
# In RStudio, p will display interactively in the Viewer pane.
}

}
