% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{heatmap.plot}
\alias{heatmap.plot}
\title{Create a Heatmap with Hierarchical Clustering}
\usage{
heatmap.plot(
  df,
  cons_genes,
  class,
  dendrogram = "both",
  show_dendrogram = c(FALSE, TRUE),
  scale = "row",
  custom_colors = c(`1` = "#2e1457", `0` = "#66a182"),
  margins = c(60, 100, 40, 20),
  grid_color = "white",
  grid_width = 1e-05,
  branches_lwd = 0.4,
  fontsize_row = 12,
  fontsize_col = 5,
  colors = NULL,
  hclust.method = "complete",
  distance.method = "euclidean",
  k_row = NULL,
  k_col = NULL,
  Rowv = TRUE,
  Colv = TRUE,
  scale_fill_gradient_fun = NULL,
  limits = NULL,
  na.value = "grey50",
  cellnote = NULL,
  cellnote_size = NULL,
  cellnote_textposition = "middle center",
  key.title = "",
  key.xlab = "Value",
  key.ylab = "Frequency"
)
}
\arguments{
\item{df}{A data frame containing gene expression data. The dataframe must include the genes of interest for the heatmap (specified in `cons_genes`).}

\item{cons_genes}{A character vector containing the names of genes to be used in the heatmap.}

\item{class}{A factor or numeric vector representing the class labels for the samples. This is used to assign colors to the side of the heatmap.}

\item{dendrogram}{A character string specifying which dendrograms to display. Possible values are "both" (both row and column), "row" (only row dendrogram), and "column" (only column dendrogram).}

\item{show_dendrogram}{A logical vector specifying whether to show the dendrogram for rows and/or columns.}

\item{scale}{A character string specifying how to scale the data for the heatmap. Options are "row", "column", or "none".}

\item{custom_colors}{A named vector specifying custom colors for the class labels. The names should match the values in `class`.}

\item{margins}{A numeric vector specifying the margins for the heatmap plot. The format is `c(bottom, left, top, right)`.}

\item{grid_color}{A color for the grid lines of the heatmap.}

\item{grid_width}{The width of the grid lines.}

\item{branches_lwd}{The line width for the branches in the dendrogram.}

\item{fontsize_row}{The font size for row labels.}

\item{fontsize_col}{The font size for column labels.}

\item{colors}{A vector of colors for the heatmap cells. Defaults to `viridis::viridis(n = 64)` if not specified.}

\item{hclust.method}{A character string specifying the hierarchical clustering method to use. Default is "complete".}

\item{distance.method}{A character string specifying the distance method for clustering. Default is "euclidean".}

\item{k_row}{The number of clusters for rows (optional).}

\item{k_col}{The number of clusters for columns (optional).}

\item{Rowv}{A logical value specifying whether to reorder rows based on hierarchical clustering. Default is `TRUE`.}

\item{Colv}{A logical value specifying whether to reorder columns based on hierarchical clustering. Default is `TRUE`.}

\item{scale_fill_gradient_fun}{A function to apply a custom gradient scale for the heatmap. Default is `NULL`.}

\item{limits}{A numeric vector specifying the color limits for the heatmap. Default is `NULL`.}

\item{na.value}{A color to use for missing values in the heatmap. Default is "grey50".}

\item{cellnote}{A matrix or data frame providing cell annotations. Default is `NULL`.}

\item{cellnote_size}{The font size for the cell annotations.}

\item{cellnote_textposition}{A string specifying the position of the text within the cell. Default is "middle center".}

\item{key.title}{A string specifying the title for the color key.}

\item{key.xlab}{A string specifying the x-axis label for the color key.}

\item{key.ylab}{A string specifying the y-axis label for the color key.}
}
\value{
A `ggplot` object representing the heatmap with hierarchical clustering.
}
\description{
This function generates a heatmap from gene expression data, with hierarchical clustering of both rows and columns. The heatmap can be customized with various parameters, such as dendrogram visibility, scaling method, color schemes, and additional annotations.
}
\examples{
\dontrun{
# Example usage
heatmap_plot <- heatmap.plot(df = gene_data, cons_genes = consensus_genes, class = class_labels)
print(heatmap_plot)
}

}
