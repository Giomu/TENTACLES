% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_testConsensus.R
\name{pca.plot}
\alias{pca.plot}
\title{Plot PCA Scores and Top Loadings for Consensus Genes}
\usage{
pca.plot(
  pca_scores,
  pca_top_loadings,
  labels,
  color_class_0 = "#3D5A80",
  color_class_1 = "#EE6C97"
)
}
\arguments{
\item{pca_scores}{Data frame with columns `PC1`, `PC2`, and optionally sample labels.}

\item{pca_top_loadings}{Data frame with columns `PC1`, `PC2`, and `gene` for top loadings.}

\item{labels}{Vector or factor indicating class/group of each sample (used for coloring).}
}
\value{
A patchwork object combining the two ggplot2 plots: sample scores and top gene loadings.
}
\description{
This function generates two plots based on precomputed PCA results:
(1) a scatterplot of sample scores on PC1 and PC2, colored by class labels,
and (2) a biplot of the top gene loadings for PC1 and PC2.
}
\details{
Use this function after running PCA on your gene expression data and extracting the relevant
scores and top gene loadings. This function does not perform PCA; it only visualizes the results.
}
\examples{
\dontrun{
# After running PCA and extracting scores/loadings:
p <- pca.plot(pca_scores = my_scores, pca_top_loadings = my_top_loadings, labels = my_labels)
print(p)
}

}
